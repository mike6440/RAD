function es = esatwat(tc)%function es = esatwat(tc)%  computes vapor pressure over water/ice%========================================================%% Input: %  tc -- column vector.%%Output: %  es (mbar or hPa) -- a column vector%%Reynolds, 981117, 010313%=======================================================%TESTclear; tc = 26;  % ans = % OVER WATERc = ones(length(tc),1) * 17.2694;d = ones(length(tc),1) * 237.3;%OVER ICEnx = find(tc < 0);c(nx) = 21.875 * ones(length(nx),1);d(nx) = 265.5 * ones(length(nx),1);% ESATes = 6.11 * ones(length(tc),1) .* exp(c .* tc  ./ (tc + d)); % mbarreturn